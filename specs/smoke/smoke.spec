# Smoke test
    Tags: smoke

Smoke test only detects if application build is launchable and it makes sense to continue running other tests on it.

## Home page loaded

* Open application
* Assert element "App" exists
* Assert element "Logo" exists
